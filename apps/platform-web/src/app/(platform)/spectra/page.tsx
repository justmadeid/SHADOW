import { ProductLanding } from "../../../shell/platform-shell";
export default function Page() {
  return (
    <ProductLanding
      product="SPECTRA"
      description="Observe and analyze activity within the same Case context."
    />
  );
}
